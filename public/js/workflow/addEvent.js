
/* if(document.getElementById("workflow")){
    let textArea = document.getElementById('workflow')
    const buttonUp = document.getElementById('button-up')
    const buttonDown = document.getElementById('button-down')
    const clearButton = document.getElementById('clear-button')
    const saveButton = document.getElementById('save-button');
buttonUp.addEventListener('click', () => { //adiciona evento ao botão de aumentar fonte
    size = toolBarNoteFunctions.fontUp(textArea, size)
    console.log(textArea, size)
})
buttonDown.addEventListener('click', ()=>{ //adiciona evento ao botão de diminuir fonte
    size = toolBarNoteFunctions.fontDown(textArea, size)
    console.log(textArea, size)
})

clearButton.addEventListener('click', () => toolBarNoteFunctions.clear(textArea)) //adiciona evento ao botão de limpar tudo

saveButton.addEventListener('click', () => toolBarNoteFunctions.save(textArea, globalVideoId))
} */
